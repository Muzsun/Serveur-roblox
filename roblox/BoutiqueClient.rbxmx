<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4"><Item class="LocalScript" referent="RBX0"><Properties><string name="Name">BoutiqueClient</string><ProtectedString name="Source"><![CDATA[-- BoutiqueClient (LocalScript dans StarterPlayer > StarterPlayerScripts)
-- La boutique de l'établi :
--  * au-dessus de chaque outil en vente : la pièce + le prix
--  * la souris passe sur l'outil : il s'entoure de blanc
--  * clic (ou E / appui) sur l'outil : la caméra se rapproche et la fiche de l'outil s'ouvre
--    (nom, description, touffes par coup, portée), avec le bouton pour acheter (ou équiper),
--    des flèches pour passer à l'autre outil, ton argent et une croix pour fermer.
-- Et la tête de la débroussailleuse qui tourne.
if not game:GetService("RunService"):IsClient() then
	warn("[Boutique] BoutiqueClient doit être un LocalScript dans StarterPlayer > StarterPlayerScripts !")
	return
end

-- outils en vente : remote du serveur, modèle sur l'établi, attribut du joueur quand c'est acheté
local ARTICLES = {
	{
		remote = "FaucilleAchat", vitrine = "FaucilleVitrine", attribut = "Faucille", nom = "Faucille", titre = "FAUCILLE",
		outil = "Faucille", -- nom de l'outil dans le sac
		desc = "Une lame en croissant bien affûtée. Fauche plusieurs touffes d'un seul coup.",
		touffesMax = 5, -- avec toutes les améliorations
		ameliorations = 3,
		hauteur = 0.95, -- caméra : 0 = de face, 1 = bien au-dessus (la faucille est posée à plat)
	},
	{
		remote = "DebroussailleuseAchat", vitrine = "DebroussailleuseVitrine", attribut = "Debroussailleuse", nom = "Débroussailleuse", titre = "DÉBROUSSAILLEUSE",
		outil = "Debroussailleuse",
		desc = "Un moteur qui rugit et un fil qui tourne à toute vitesse. Rien ne lui résiste !",
		touffesMax = 10,
		ameliorations = 3,
		hauteur = 0.12, -- accrochée au mur : presque de face
	},
}
local REGLAGES = {
	TAILLE_PRIX = 0.75, -- hauteur du prix flottant (studs)
	DISTANCE = 22, -- on peut cliquer sur un outil jusqu'à cette distance
	DUREE_CAMERA = 0.6, -- durée du rapprochement de la caméra (secondes)
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local GuiService = game:GetService("GuiService")

local COIN = "rbxassetid://103593634628600" -- l'icône des pièces
local HERBE = "rbxassetid://90182525256912" -- l'image d'une touffe d'herbe
local lp = Players.LocalPlayer
local pgui = lp:WaitForChild("PlayerGui")
-- si le script est installé 2 fois, les 2 copies se cachent l'une l'autre : on n'en garde qu'une
if pgui:FindFirstChild("Boutique") or lp:GetAttribute("BoutiqueLancee") then
	warn("[Boutique] BoutiqueClient est installé 2 fois ! Garde un seul BoutiqueClient dans StarterPlayer > StarterPlayerScripts (et plus de FaucilleClient).")
	return
end
lp:SetAttribute("BoutiqueLancee", true)
local cam = workspace.CurrentCamera

local BLANC, ENCRE = Color3.new(1, 1, 1), Color3.fromRGB(26, 22, 28)
local OR, VERT, ROUGE = Color3.fromRGB(255, 215, 70), Color3.fromRGB(110, 224, 74), Color3.fromRGB(232, 50, 58)

local function argentTexte(v) return string.format("$%.2f", v) end
local function coin(o, r) local c = Instance.new("UICorner") c.CornerRadius = r or UDim.new(1, 0) c.Parent = o return c end
local function contour(o, e, c, bord)
	local s = Instance.new("UIStroke")
	s.Thickness = e
	s.Color = c or ENCRE
	s.LineJoinMode = Enum.LineJoinMode.Round
	if bord then s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border end
	s.Parent = o
	return s
end
local function nouveau(classe, props, parent)
	local o = Instance.new(classe)
	for k, v in props do o[k] = v end
	o.Parent = parent
	return o
end

-- argent du joueur (même recherche que le reste du jeu)
local okCfg, CFG = pcall(function() return require(ReplicatedStorage:WaitForChild("GrassConfig", 10)) end)
if not okCfg or type(CFG) ~= "table" then CFG = {} end
local function argent()
	local ls = lp:FindFirstChild("leaderstats")
	if not ls then return 0 end
	local v
	if CFG.MONEY_STAT and CFG.MONEY_STAT ~= "" then v = ls:FindFirstChild(CFG.MONEY_STAT) end
	if not v then
		for _, n in { "Money", "Cash", "Argent", "Coins", "Dollars", "Pieces", "Pièces", "$" } do
			v = ls:FindFirstChild(n)
			if v then break end
		end
	end
	return v and tonumber(v.Value) or 0
end

---------------------------------------------------------------- Les outils sur l'établi
local vitrines = {} -- liste des outils trouvés : { article, achat, modele, base, centre, taille, repere, etiquette, contour }
local ouvert = false

-- boîte autour des pièces visibles de l'outil (dans le repère de l'établi)
local function mesurer(modele, base)
	local rep = base.CFrame.Rotation
	local mini, maxi = Vector3.new(math.huge, math.huge, math.huge), -Vector3.new(math.huge, math.huge, math.huge)
	for _, p in modele:GetDescendants() do
		if p:IsA("BasePart") and p.Transparency < 1 then
			local l = rep:PointToObjectSpace(p.Position)
			local r = p.Size.Magnitude / 2 * 0.6
			mini = Vector3.new(math.min(mini.X, l.X - r), math.min(mini.Y, l.Y - r), math.min(mini.Z, l.Z - r))
			maxi = Vector3.new(math.max(maxi.X, l.X + r), math.max(maxi.Y, l.Y + r), math.max(maxi.Z, l.Z + r))
		end
	end
	if mini.X == math.huge then return base.CFrame, base.Size end
	return rep * CFrame.new((mini + maxi) / 2), maxi - mini
end

-- un rayon touche-t-il la boîte ? (renvoie la distance)
local function rayonBoite(origine, direction, cf, taille)
	local o = cf:PointToObjectSpace(origine)
	local d = cf:VectorToObjectSpace(direction)
	local tmin, tmax = 0, math.huge
	for _, ax in { "X", "Y", "Z" } do
		local h = taille[ax] / 2
		if math.abs(d[ax]) < 1e-6 then
			if o[ax] < -h or o[ax] > h then return nil end
		else
			local t1, t2 = (-h - o[ax]) / d[ax], (h - o[ax]) / d[ax]
			if t1 > t2 then t1, t2 = t2, t1 end
			tmin, tmax = math.max(tmin, t1), math.min(tmax, t2)
			if tmin > tmax then return nil end
		end
	end
	return tmin
end

-- le prix flottant au-dessus de l'outil : [pièce] $7.99
local function creerEtiquette(v)
	local at = Instance.new("Attachment")
	at.Name = "Prix_" .. v.article.attribut
	at.Parent = workspace.Terrain
	local devant = v.base.CFrame.LookVector
	at.WorldPosition = v.centre.Position + Vector3.new(0, v.taille.Y / 2 + 0.55, 0) + devant * 0.35
	local g = nouveau("BillboardGui", {
		Name = "Prix_" .. v.article.attribut, Adornee = at, AlwaysOnTop = true, LightInfluence = 0, MaxDistance = 45,
		ResetOnSpawn = false, Size = UDim2.fromScale(REGLAGES.TAILLE_PRIX * 3.6, REGLAGES.TAILLE_PRIX),
	}, pgui)
	local root = nouveau("Frame", { BackgroundTransparency = 1, AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromScale(0.5, 0.5), Size = UDim2.fromOffset(360, 100) }, g)
	local echelle = nouveau("UIScale", {}, root)
	nouveau("UIListLayout", {
		FillDirection = Enum.FillDirection.Horizontal, HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Center, Padding = UDim.new(0, 10), SortOrder = Enum.SortOrder.LayoutOrder,
	}, root)
	local piece = nouveau("ImageLabel", { BackgroundTransparency = 1, Image = COIN, ScaleType = Enum.ScaleType.Fit, Size = UDim2.fromOffset(84, 84), LayoutOrder = 1 }, root)
	local texte = nouveau("TextLabel", {
		BackgroundTransparency = 1, Font = Enum.Font.LuckiestGuy, TextSize = 76, TextColor3 = BLANC, Text = argentTexte(v.prix()),
		Size = UDim2.fromOffset(0, 90), AutomaticSize = Enum.AutomaticSize.X, LayoutOrder = 2,
	}, root)
	contour(texte, 6)
	v.etiquette = { g = g, echelle = echelle, piece = piece, texte = texte, at = at }
end

local function majEtiquette(v)
	local e = v.etiquette
	e.g.Enabled = not ouvert and not lp:GetAttribute("IntroEnCours")
	if not e.g.Enabled then return end
	e.echelle.Scale = e.g.AbsoluteSize.Y / 100
	if lp:GetAttribute(v.article.attribut) then
		e.piece.Visible = false
		e.texte.Text = "✓"
		e.texte.TextColor3 = VERT
	else
		e.piece.Visible = true
		e.texte.Text = argentTexte(v.prix())
		e.texte.TextColor3 = BLANC
	end
end

---------------------------------------------------------------- La fiche de l'outil (vue rapprochée)
-- Même style que le menu BOOSTS : carte verte, lignes crème, textes blancs contournés, boutons en relief.
local ecran = nouveau("ScreenGui", { Name = "Boutique", ResetOnSpawn = false, IgnoreGuiInset = true, DisplayOrder = 30, Enabled = false }, pgui)
local echelles = {} -- tout est dessiné pour un écran de 1080 px de haut, puis mis à l'échelle
local function echelle(o) table.insert(echelles, nouveau("UIScale", {}, o)) return o end
local function adapter()
	local s = math.clamp(cam.ViewportSize.Y / 1080, 0.45, 1.25)
	for _, u in echelles do u.Scale = s end
end
local function degrade(o, couleurs, rotation)
	local k = {}
	for i, c in couleurs do table.insert(k, ColorSequenceKeypoint.new((i - 1) / (#couleurs - 1), c)) end
	return nouveau("UIGradient", { Rotation = rotation or 90, Color = ColorSequence.new(k) }, o)
end
local function titre(parent, texte, props)
	local l = nouveau("TextLabel", {
		BackgroundTransparency = 1, Font = Enum.Font.LuckiestGuy, TextColor3 = BLANC, Text = texte, TextScaled = false,
	}, parent)
	for k, v in props do if k ~= "epais" then l[k] = v end end
	contour(l, props.epais or 4)
	return l
end
-- libère la souris (même en vue 1re personne) tant que la fiche est ouverte
nouveau("TextButton", { Text = "", BackgroundTransparency = 1, Size = UDim2.fromOffset(1, 1), Modal = true, Selectable = false }, ecran)

-- fond assombri à gauche (la fiche se lit mieux, l'outil reste bien visible à droite)
local voile = nouveau("Frame", { Size = UDim2.fromScale(1, 1), BackgroundColor3 = Color3.fromRGB(8, 18, 10), BorderSizePixel = 0, BackgroundTransparency = 0 }, ecran)
nouveau("UIGradient", {
	Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.45), NumberSequenceKeypoint.new(0.35, 0.72), NumberSequenceKeypoint.new(0.58, 1), NumberSequenceKeypoint.new(1, 1),
	}),
}, voile)

-- la carte
local carte = echelle(nouveau("Frame", { Name = "Fiche", AnchorPoint = Vector2.new(0, 0.5), Position = UDim2.new(0, 50, 0.5, 0), Size = UDim2.fromOffset(510, 760) }, ecran))
coin(carte, UDim.new(0, 32))
contour(carte, 5, ENCRE, true)
degrade(carte, { Color3.fromRGB(66, 124, 74), Color3.fromRGB(30, 66, 42) })
local bordInt = nouveau("Frame", { AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromScale(0.5, 0.5), Size = UDim2.new(1, -16, 1, -16), BackgroundTransparency = 1 }, carte)
coin(bordInt, UDim.new(0, 26))
contour(bordInt, 2, BLANC, true).Transparency = 0.7
local reflet = nouveau("Frame", { AnchorPoint = Vector2.new(0.5, 0), Position = UDim2.new(0.5, 0, 0, 9), Size = UDim2.new(1, -24, 0, 96), BackgroundColor3 = BLANC, BorderSizePixel = 0 }, carte)
coin(reflet, UDim.new(0, 24))
nouveau("UIGradient", { Rotation = 90, Transparency = NumberSequence.new(0.8, 1) }, reflet)
-- petite étiquette "OUTIL"
local etiq = nouveau("Frame", { AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.new(0.5, 0, 0, 2), Size = UDim2.fromOffset(150, 48), Rotation = -2, ZIndex = 3 }, carte)
coin(etiq, UDim.new(0, 18))
contour(etiq, 4, ENCRE, true)
degrade(etiq, { Color3.fromRGB(255, 226, 112), Color3.fromRGB(240, 160, 32) })
titre(etiq, "OUTIL", { Size = UDim2.fromScale(1, 1), Position = UDim2.fromOffset(0, 3), TextSize = 28, ZIndex = 4, epais = 3 })
-- nom + description
local nomOutil = titre(carte, "", { Position = UDim2.fromOffset(10, 42), Size = UDim2.new(1, -20, 0, 76), TextSize = 62, epais = 5 })
nouveau("UITextSizeConstraint", { MaxTextSize = 62 }, nomOutil)
nomOutil.TextScaled = true
degrade(nomOutil, { BLANC, Color3.fromRGB(215, 255, 190) })
local descOutil = nouveau("TextLabel", {
	BackgroundTransparency = 1, Position = UDim2.fromOffset(34, 126), Size = UDim2.new(1, -68, 0, 96), Font = Enum.Font.FredokaOne, TextSize = 26,
	TextWrapped = true, TextColor3 = Color3.fromRGB(226, 242, 216), Text = "",
}, carte)

-- une ligne crème (comme dans BOOSTS)
local function ligne(y, h)
	local l = nouveau("Frame", { Position = UDim2.fromOffset(27, y), Size = UDim2.new(1, -54, 0, h) }, carte)
	coin(l, UDim.new(0, 24))
	contour(l, 4, ENCRE, true)
	degrade(l, { Color3.fromRGB(252, 249, 240), Color3.fromRGB(236, 230, 212) })
	return l, nouveau("UIScale", {}, l)
end
local VERT_FONCE = Color3.fromRGB(60, 90, 64)
-- touffes par coup (avec des petites touffes dessinées)
local ligneTouffes, popTouffes = ligne(230, 132)
nouveau("TextLabel", { BackgroundTransparency = 1, Position = UDim2.fromOffset(20, 12), Size = UDim2.fromOffset(300, 32), Font = Enum.Font.LuckiestGuy, TextSize = 26, TextXAlignment = Enum.TextXAlignment.Left, TextColor3 = VERT_FONCE, Text = "TOUFFES PAR COUP" }, ligneTouffes)
local valTouffes = titre(ligneTouffes, "3", { AnchorPoint = Vector2.new(1, 0), Position = UDim2.new(1, -18, 0, 4), Size = UDim2.fromOffset(90, 60), TextSize = 58, TextXAlignment = Enum.TextXAlignment.Right, epais = 5 })
local rangee = nouveau("Frame", { BackgroundTransparency = 1, Position = UDim2.fromOffset(16, 64), Size = UDim2.new(1, -32, 0, 50) }, ligneTouffes)
nouveau("UIListLayout", { FillDirection = Enum.FillDirection.Horizontal, Padding = UDim.new(0, 2), VerticalAlignment = Enum.VerticalAlignment.Center, SortOrder = Enum.SortOrder.LayoutOrder }, rangee)
local touffesMax = nouveau("TextLabel", {
	BackgroundTransparency = 1, AnchorPoint = Vector2.new(1, 1), Position = UDim2.new(1, -18, 1, -10), Size = UDim2.fromOffset(250, 26),
	Font = Enum.Font.FredokaOne, TextSize = 19, TextXAlignment = Enum.TextXAlignment.Right, TextColor3 = Color3.fromRGB(138, 130, 112), Text = "",
}, ligneTouffes)
-- portée (barre)
local lignePortee, popPortee = ligne(378, 112)
nouveau("TextLabel", { BackgroundTransparency = 1, Position = UDim2.fromOffset(20, 12), Size = UDim2.fromOffset(300, 32), Font = Enum.Font.LuckiestGuy, TextSize = 26, TextXAlignment = Enum.TextXAlignment.Left, TextColor3 = VERT_FONCE, Text = "PORTÉE" }, lignePortee)
local valPortee = titre(lignePortee, "", { AnchorPoint = Vector2.new(1, 0), Position = UDim2.new(1, -18, 0, 6), Size = UDim2.fromOffset(110, 50), TextSize = 44, TextXAlignment = Enum.TextXAlignment.Right, epais = 4 })
local barre = nouveau("Frame", { Position = UDim2.new(0, 20, 1, -44), Size = UDim2.new(1, -150, 0, 26), BackgroundColor3 = Color3.fromRGB(222, 214, 194), ClipsDescendants = true }, lignePortee)
coin(barre)
contour(barre, 2.5, Color3.fromRGB(122, 106, 86), true)
local remplissage = nouveau("Frame", { Size = UDim2.fromScale(0.5, 1), BackgroundColor3 = BLANC, BorderSizePixel = 0 }, barre)
coin(remplissage)
degrade(remplissage, { Color3.fromRGB(168, 240, 122), Color3.fromRGB(60, 174, 68) })
-- améliorations disponibles
local pilule = nouveau("Frame", { Position = UDim2.fromOffset(27, 508), Size = UDim2.new(1, -54, 0, 60), BackgroundColor3 = Color3.fromRGB(14, 34, 20), BackgroundTransparency = 0.35 }, carte)
coin(pilule)
contour(pilule, 3, ENCRE, true)
local texteAmelio = titre(pilule, "", { Size = UDim2.fromScale(1, 1), Position = UDim2.fromOffset(0, 3), TextSize = 25, epais = 3 })

-- le gros bouton (acheter / équiper), en relief
local bouton = nouveau("TextButton", { Name = "Acheter", Text = "", AutoButtonColor = false, BackgroundTransparency = 1, AnchorPoint = Vector2.new(0, 1), Position = UDim2.new(0, 27, 1, -30), Size = UDim2.new(1, -54, 0, 122) }, carte)
local socle = nouveau("Frame", { Position = UDim2.fromOffset(0, 12), Size = UDim2.new(1, 0, 1, -12) }, bouton)
coin(socle, UDim.new(0, 32))
contour(socle, 4, ENCRE, true)
local socleGrad = degrade(socle, { BLANC, BLANC })
local face = nouveau("Frame", { Size = UDim2.new(1, 0, 1, -16) }, bouton)
coin(face, UDim.new(0, 32))
contour(face, 4, ENCRE, true)
local faceGrad = degrade(face, { BLANC, BLANC })
local brillance = nouveau("Frame", { AnchorPoint = Vector2.new(0.5, 0), Position = UDim2.new(0.5, 0, 0, 6), Size = UDim2.new(1, -20, 0.4, 0), BackgroundColor3 = BLANC, BorderSizePixel = 0 }, face)
coin(brillance)
nouveau("UIGradient", { Rotation = 90, Transparency = NumberSequence.new(0.5, 1) }, brillance)
local contenu = nouveau("Frame", { BackgroundTransparency = 1, Size = UDim2.fromScale(1, 1) }, face)
nouveau("UIListLayout", { FillDirection = Enum.FillDirection.Horizontal, HorizontalAlignment = Enum.HorizontalAlignment.Center, VerticalAlignment = Enum.VerticalAlignment.Center, Padding = UDim.new(0, 10), SortOrder = Enum.SortOrder.LayoutOrder }, contenu)
local pieceBouton = nouveau("ImageLabel", { BackgroundTransparency = 1, Image = COIN, ScaleType = Enum.ScaleType.Fit, Size = UDim2.fromOffset(62, 62), LayoutOrder = 1 }, contenu)
local texteBouton = nouveau("TextLabel", { BackgroundTransparency = 1, Font = Enum.Font.LuckiestGuy, TextSize = 62, TextColor3 = BLANC, Text = "", Size = UDim2.fromOffset(0, 70), AutomaticSize = Enum.AutomaticSize.X, LayoutOrder = 2 }, contenu)
contour(texteBouton, 5)
local popBouton = nouveau("UIScale", {}, bouton)
local STYLES = {
	vert = { { Color3.fromRGB(168, 244, 120), Color3.fromRGB(56, 170, 62) }, { Color3.fromRGB(36, 112, 42), Color3.fromRGB(20, 68, 26) } },
	gris = { { Color3.fromRGB(214, 210, 198), Color3.fromRGB(160, 156, 144) }, { Color3.fromRGB(128, 124, 114), Color3.fromRGB(96, 92, 84) } },
	or_ = { { Color3.fromRGB(255, 232, 120), Color3.fromRGB(240, 160, 30) }, { Color3.fromRGB(170, 100, 12), Color3.fromRGB(110, 62, 6) } },
}
local function peindre(style)
	local s = STYLES[style]
	faceGrad.Color = ColorSequence.new(s[1][1], s[1][2])
	socleGrad.Color = ColorSequence.new(s[2][1], s[2][2])
end
bouton.MouseButton1Down:Connect(function() face.Position = UDim2.fromOffset(0, 8) end)
bouton.MouseButton1Up:Connect(function() face.Position = UDim2.fromOffset(0, 0) end)

-- navigation entre les outils : ‹ 1 / 2 ›
local nav = echelle(nouveau("Frame", { BackgroundTransparency = 1, AnchorPoint = Vector2.new(0.5, 1), Position = UDim2.new(0.66, 0, 1, -36), Size = UDim2.fromOffset(340, 96) }, ecran))
nouveau("UIListLayout", { FillDirection = Enum.FillDirection.Horizontal, HorizontalAlignment = Enum.HorizontalAlignment.Center, VerticalAlignment = Enum.VerticalAlignment.Center, Padding = UDim.new(0, 18), SortOrder = Enum.SortOrder.LayoutOrder }, nav)
local function rond(txt, ordre)
	local b = nouveau("TextButton", { Text = "", AutoButtonColor = false, Size = UDim2.fromOffset(88, 88), LayoutOrder = ordre }, nav)
	coin(b)
	contour(b, 4, ENCRE, true)
	degrade(b, { Color3.fromRGB(168, 244, 120), Color3.fromRGB(56, 170, 62) })
	titre(b, txt, { Size = UDim2.fromScale(1, 1), Position = UDim2.fromOffset(0, 5), TextSize = 46, epais = 4 })
	return b
end
local gauche = rond("<", 1)
local compteurFond = nouveau("Frame", { Size = UDim2.fromOffset(120, 58), BackgroundColor3 = Color3.fromRGB(14, 34, 20), BackgroundTransparency = 0.1, LayoutOrder = 2 }, nav)
coin(compteurFond)
contour(compteurFond, 4, ENCRE, true)
local compteur = titre(compteurFond, "1 / 2", { Size = UDim2.fromScale(1, 1), Position = UDim2.fromOffset(0, 3), TextSize = 34, epais = 3 })
local droite = rond(">", 3)

-- ton argent (en haut à droite)
local cadreArgent = echelle(nouveau("Frame", { AnchorPoint = Vector2.new(1, 0), Position = UDim2.new(1, -130, 0, 34), Size = UDim2.fromOffset(300, 74), BackgroundColor3 = Color3.fromRGB(14, 34, 20), BackgroundTransparency = 0.05 }, ecran))
coin(cadreArgent)
contour(cadreArgent, 4, ENCRE, true)
nouveau("ImageLabel", { BackgroundTransparency = 1, Image = COIN, ScaleType = Enum.ScaleType.Fit, AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromOffset(18, 37), Size = UDim2.fromOffset(94, 94) }, cadreArgent)
local texteArgent = titre(cadreArgent, "$0.00", { Position = UDim2.fromOffset(70, 6), Size = UDim2.new(1, -90, 1, -6), TextSize = 46, TextXAlignment = Enum.TextXAlignment.Right, epais = 4 })
degrade(texteArgent, { BLANC, Color3.fromRGB(255, 228, 140) })

-- fermer (rond rouge avec une croix)
local boutonX = echelle(nouveau("TextButton", { Text = "", AutoButtonColor = false, AnchorPoint = Vector2.new(1, 0), Position = UDim2.new(1, -30, 0, 28), Size = UDim2.fromOffset(86, 86) }, ecran))
coin(boutonX)
contour(boutonX, 4, ENCRE, true)
degrade(boutonX, { Color3.fromRGB(255, 150, 140), Color3.fromRGB(235, 60, 55), Color3.fromRGB(160, 24, 30) })
for couche = 1, 2 do
	for _, rot in { 45, -45 } do
		local b = nouveau("Frame", {
			AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromScale(0.5, 0.5), Rotation = rot, BorderSizePixel = 0,
			Size = couche == 1 and UDim2.fromScale(0.62, 0.22) or UDim2.fromScale(0.52, 0.12), BackgroundColor3 = couche == 1 and ENCRE or BLANC,
		}, boutonX)
		coin(b)
	end
end

-- boutons qui grossissent un peu au survol
for _, b in { gauche, droite, boutonX, bouton } do
	local sc = if b == bouton then popBouton else nouveau("UIScale", {}, b:IsA("GuiButton") and b or b)
	if b == boutonX then
		-- boutonX a déjà l'échelle de l'écran : on grossit sa croix à la place
		sc = nil
	end
	if not sc then continue end
	b.MouseEnter:Connect(function() TweenService:Create(sc, TweenInfo.new(0.12), { Scale = 1.06 }):Play() end)
	b.MouseLeave:Connect(function() TweenService:Create(sc, TweenInfo.new(0.12), { Scale = 1 }):Play() face.Position = UDim2.fromOffset(0, 0) end)
end
adapter()
cam:GetPropertyChangedSignal("ViewportSize"):Connect(adapter)

-- messages au centre de l'écran (achat réussi, argent qui manque)
local messages = nouveau("ScreenGui", { Name = "BoutiqueMessages", ResetOnSpawn = false, DisplayOrder = 40 }, pgui)
local function son(id, vol)
	local s = nouveau("Sound", { SoundId = id, Volume = vol or 0.6 }, messages)
	s:Play()
	game:GetService("Debris"):AddItem(s, 4)
end
local function message(t, couleur, duree)
	local l = nouveau("TextLabel", {
		BackgroundTransparency = 1, AnchorPoint = Vector2.new(0.5, 0.5), Position = UDim2.fromScale(0.5, 0.3), Size = UDim2.fromScale(0.7, 0.1),
		Font = Enum.Font.LuckiestGuy, TextScaled = true, Text = t, TextColor3 = couleur,
	}, messages)
	local st = contour(l, 5)
	local sc = nouveau("UIScale", { Scale = 0 }, l)
	TweenService:Create(sc, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Scale = 1 }):Play()
	task.delay(duree, function()
		TweenService:Create(l, TweenInfo.new(0.3), { TextTransparency = 1, Position = UDim2.fromScale(0.5, 0.25) }):Play()
		TweenService:Create(st, TweenInfo.new(0.3), { Transparency = 1 }):Play()
		task.wait(0.35)
		l:Destroy()
	end)
end
local function trembler(o)
	task.spawn(function()
		local p0 = o.Position
		for i = 1, 6 do
			o.Position = p0 + UDim2.fromOffset(i % 2 == 0 and -8 or 8, 0)
			task.wait(0.04)
		end
		o.Position = p0
	end)
end

---------------------------------------------------------------- Caméra
local tourCamera = 0
local function bougerCamera(vers, duree, fin)
	tourCamera += 1
	local id = tourCamera
	local depart = cam.CFrame
	task.spawn(function()
		local t0 = os.clock()
		while tourCamera == id do
			local a = math.min((os.clock() - t0) / duree, 1)
			local e = if a < 0.5 then 4 * a * a * a else 1 - (-2 * a + 2) ^ 3 / 2 -- doux au début et à la fin
			cam.CFrame = depart:Lerp(vers, e)
			if a >= 1 then break end
			RunService.RenderStepped:Wait()
		end
		if tourCamera == id and fin then fin() end
	end)
end
-- où placer la caméra pour bien voir l'outil (un peu décalé : la fiche prend la gauche de l'écran)
local function cadrage(v)
	local centre = v.centre.Position
	local devant = v.base.CFrame.LookVector
	local ch = lp.Character
	local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
	if hrp and (hrp.Position - centre):Dot(devant) < 0 then devant = -devant end -- du côté où se trouve le joueur
	devant = Vector3.new(devant.X, 0, devant.Z).Unit
	local largeur = math.max(v.taille.X, v.taille.Z, 1.5)
	local distance = math.max(largeur * 0.95, 3)
	local droite = (-devant):Cross(Vector3.yAxis).Unit -- la droite de l'écran
	local cible = centre - droite * largeur * 0.3
	local dir = (devant + Vector3.yAxis * v.article.hauteur).Unit
	return CFrame.lookAt(cible + dir * distance, cible)
end

---------------------------------------------------------------- Ouvrir / fermer la fiche
local courant = 1 -- outil affiché
local etat = {}
local fermeture = false

local function outilEnMain(v)
	local ch = lp.Character
	local t = ch and ch:FindFirstChildOfClass("Tool")
	return t ~= nil and t.Name == v.article.outil
end
local function styleBouton(v)
	if lp:GetAttribute(v.article.attribut) then
		-- déjà achetée : le bouton sert à la prendre en main
		peindre("or_")
		pieceBouton.Visible = false
		texteBouton.Text = outilEnMain(v) and "✓ EN MAIN" or "ÉQUIPER"
		texteBouton.TextSize = 54
	else
		peindre(argent() + 1e-6 >= v.prix() and "vert" or "gris")
		pieceBouton.Visible = true
		texteBouton.Text = argentTexte(v.prix())
		texteBouton.TextSize = 62
	end
end

local function remplir(v)
	local a = v.article
	nomOutil.Text = a.titre
	descOutil.Text = a.desc
	local n = tonumber(v.achat:GetAttribute("Touffes")) or 1
	valTouffes.Text = tostring(n)
	touffesMax.Text = a.touffesMax and ("jusqu'à " .. a.touffesMax .. " avec les boosts") or ""
	for _, c in rangee:GetChildren() do if c:IsA("ImageLabel") then c:Destroy() end end
	local taille = math.min(46, math.floor(300 / math.max(n, 1)))
	for i = 1, math.min(n, 12) do
		local im = nouveau("ImageLabel", { BackgroundTransparency = 1, Image = HERBE, ScaleType = Enum.ScaleType.Fit, Size = UDim2.fromOffset(taille, taille), LayoutOrder = i }, rangee)
		local sc = nouveau("UIScale", { Scale = 0 }, im)
		task.delay(0.25 + i * 0.05, function()
			TweenService:Create(sc, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Scale = 1 }):Play()
		end)
	end
	local rayon = tonumber(v.achat:GetAttribute("Rayon")) or 5
	valPortee.Text = (rayon == math.floor(rayon)) and tostring(rayon) or string.format("%.1f", rayon)
	remplissage.Size = UDim2.fromScale(0, 1)
	TweenService:Create(remplissage, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = UDim2.fromScale(math.clamp(rayon / 10, 0.08, 1), 1) }):Play()
	texteAmelio.Text = (a.ameliorations or 3) .. " AMÉLIORATIONS DANS BOOSTS"
	compteur.Text = courant .. " / " .. #vitrines
	nav.Visible = #vitrines > 1
	styleBouton(v)
end

local controles
local vitesseAvant
local function bloquerControles(bloquer)
	-- ton jeu n'a pas forcément le "PlayerModule" de Roblox : on ne l'attend pas (sinon tout se bloque)
	if not controles then
		local ps = lp:FindFirstChild("PlayerScripts")
		local pm = ps and ps:FindFirstChild("PlayerModule")
		if pm then pcall(function() controles = require(pm):GetControls() end) end
	end
	if controles then pcall(function() if bloquer then controles:Disable() else controles:Enable() end end) end
	-- dans tous les cas : le personnage ne bouge plus pendant la fiche
	local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
	if h then
		if bloquer then
			vitesseAvant = vitesseAvant or { h.WalkSpeed, h.JumpPower, h.JumpHeight }
			h.WalkSpeed, h.JumpPower, h.JumpHeight = 0, 0, 0
		elseif vitesseAvant then
			h.WalkSpeed, h.JumpPower, h.JumpHeight = vitesseAvant[1], vitesseAvant[2], vitesseAvant[3]
			vitesseAvant = nil
		end
	end
end

-- souris libre (même si le jeu est en vue 1re personne) : réglée après les scripts de caméra, à chaque image
local function libererSouris(oui)
	pcall(function() RunService:UnbindFromRenderStep("BoutiqueSouris") end)
	if oui then
		RunService:BindToRenderStep("BoutiqueSouris", Enum.RenderPriority.Last.Value, function()
			UIS.MouseBehavior = Enum.MouseBehavior.Default
			UIS.MouseIconEnabled = true
		end)
	end
end

-- les effets de flou du jeu (profondeur de champ, flou) sont coupés pendant la fiche : l'outil reste net
local flous = {}
local function couperFlou(oui)
	if oui then
		flous = {}
		for _, conteneur in { game:GetService("Lighting"), cam } do
			for _, e in conteneur:GetChildren() do
				if (e:IsA("DepthOfFieldEffect") or e:IsA("BlurEffect")) and e.Enabled then
					e.Enabled = false
					table.insert(flous, e)
				end
			end
		end
	else
		for _, e in flous do if e.Parent then e.Enabled = true end end
		flous = {}
	end
end

-- remet tout comme avant (aussi utilisé si une erreur arrive pendant l'ouverture)
local function restaurer()
	tourCamera += 1
	ecran.Enabled = false
	cam.CameraType = Enum.CameraType.Custom
	cam.FieldOfView = etat.fov or 70
	for _, g in etat.guis or {} do if g.Parent then g.Enabled = true end end
	etat.guis = {}
	pcall(function() game:GetService("ProximityPromptService").Enabled = true end)
	lp:SetAttribute("MenuOuvert", false)
	bloquerControles(false)
	libererSouris(false)
	couperFlou(false)
	ouvert, fermeture = false, false
end

local function ouvrirInterne(v)
	ouvert = true
	couperFlou(true)
	libererSouris(true)
	for i, x in vitrines do if x == v then courant = i end end
	etat.cf, etat.type, etat.fov = cam.CFrame, cam.CameraType, cam.FieldOfView
	-- on cache le reste de l'interface et on bloque le personnage
	etat.guis = {}
	for _, g in pgui:GetChildren() do
		if g:IsA("ScreenGui") and g ~= ecran and g ~= messages and g.Name ~= "Boutique" and g.Name ~= "BoutiqueMessages" and g.Enabled then
			g.Enabled = false
			table.insert(etat.guis, g)
		end
	end
	pcall(function() game:GetService("ProximityPromptService").Enabled = false end)
	lp:SetAttribute("MenuOuvert", true) -- pas de coupe pendant ce temps
	bloquerControles(true)
	for _, x in vitrines do x.contour.Enabled = false end
	-- la caméra se rapproche de l'outil
	local okC, cible = pcall(cadrage, v)
	if okC then
		cam.CameraType = Enum.CameraType.Scriptable
		cam.FieldOfView = 70
		bougerCamera(cible, REGLAGES.DUREE_CAMERA)
	else
		warn("[Boutique] Erreur de caméra : " .. tostring(cible))
	end
	-- la fiche arrive
	ecran.Enabled = true
	local okR, errR = pcall(remplir, v)
	if not okR then warn("[Boutique] Erreur en remplissant la fiche : " .. tostring(errR)) end
	voile.BackgroundTransparency = 1
	carte.Position = UDim2.new(0, -620, 0.5, 0)
	nav.Position = UDim2.new(0.66, 0, 1, 200)
	cadreArgent.Position = UDim2.new(1, -130, 0, -140)
	boutonX.Position = UDim2.new(1, -30, 0, -140)
	local info = TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
	TweenService:Create(voile, TweenInfo.new(0.4), { BackgroundTransparency = 0 }):Play()
	task.delay(0.12, function()
		TweenService:Create(carte, info, { Position = UDim2.new(0, 50, 0.5, 0) }):Play()
		TweenService:Create(nav, info, { Position = UDim2.new(0.66, 0, 1, -36) }):Play()
		TweenService:Create(cadreArgent, info, { Position = UDim2.new(1, -130, 0, 34) }):Play()
		TweenService:Create(boutonX, info, { Position = UDim2.new(1, -30, 0, 28) }):Play()
	end)
	if UIS.GamepadEnabled then GuiService.SelectedObject = bouton end
end

local function ouvrir(v)
	if ouvert or fermeture or not v then return end
	local ok, err = pcall(ouvrirInterne, v)
	if not ok then
		warn("[Boutique] Erreur à l'ouverture de la fiche : " .. tostring(err))
		restaurer()
	end
end

local function fermer()
	if not ouvert or fermeture then return end
	fermeture = true
	GuiService.SelectedObject = nil
	local info = TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
	TweenService:Create(carte, info, { Position = UDim2.new(0, -620, 0.5, 0) }):Play()
	TweenService:Create(nav, info, { Position = UDim2.new(0.66, 0, 1, 200) }):Play()
	TweenService:Create(cadreArgent, info, { Position = UDim2.new(1, -130, 0, -140) }):Play()
	TweenService:Create(boutonX, info, { Position = UDim2.new(1, -30, 0, -140) }):Play()
	TweenService:Create(voile, TweenInfo.new(0.35), { BackgroundTransparency = 1 }):Play()
	-- la caméra revient au joueur
	bougerCamera(etat.cf, 0.45, function()
		ecran.Enabled = false
		cam.CameraType = etat.type or Enum.CameraType.Custom
		cam.FieldOfView = etat.fov or 70
		for _, g in etat.guis or {} do if g.Parent then g.Enabled = true end end
		pcall(function() game:GetService("ProximityPromptService").Enabled = true end)
		lp:SetAttribute("MenuOuvert", false)
		bloquerControles(false)
		libererSouris(false)
		couperFlou(false)
		ouvert, fermeture = false, false
	end)
end

local function changer(delta)
	if not ouvert or fermeture or #vitrines < 2 then return end
	courant = (courant - 1 + delta) % #vitrines + 1
	local v = vitrines[courant]
	bougerCamera(cadrage(v), 0.5)
	-- la carte sort par la gauche et revient avec le nouvel outil
	TweenService:Create(carte, TweenInfo.new(0.14, Enum.EasingStyle.Quad, Enum.EasingDirection.In), { Position = UDim2.new(0, -620, 0.5, 0) }):Play()
	task.delay(0.15, function()
		remplir(v)
		TweenService:Create(carte, TweenInfo.new(0.38, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Position = UDim2.new(0, 50, 0.5, 0) }):Play()
	end)
end

local function acheter()
	local v = vitrines[courant]
	if not v then return end
	if lp:GetAttribute(v.article.attribut) then
		-- déjà achetée : on la prend en main et on retourne jouer
		local sac = lp:FindFirstChildOfClass("Backpack")
		local t = sac and sac:FindFirstChild(v.article.outil)
		local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
		if t and h then h:EquipTool(t) end
		fermer()
		return
	end
	if argent() + 1e-6 < v.prix() then
		trembler(bouton)
		message("Il te manque " .. argentTexte(v.prix() - argent()), ROUGE, 1.6)
		return
	end
	v.achat:FireServer("acheter")
end

bouton.Activated:Connect(acheter)
boutonX.Activated:Connect(fermer)
gauche.Activated:Connect(function() changer(-1) end)
droite.Activated:Connect(function() changer(1) end)

UIS.InputBegan:Connect(function(input, gp)
	if ouvert then
		local k = input.KeyCode
		if k == Enum.KeyCode.Left or k == Enum.KeyCode.A or k == Enum.KeyCode.DPadLeft then changer(-1)
		elseif k == Enum.KeyCode.Right or k == Enum.KeyCode.D or k == Enum.KeyCode.DPadRight then changer(1)
		elseif k == Enum.KeyCode.Backspace or k == Enum.KeyCode.ButtonB or k == Enum.KeyCode.X then fermer() end
		return
	end
	if gp then return end
	-- clic / appui sur un outil de l'établi
	local t = input.UserInputType
	if t == Enum.UserInputType.MouseButton1 or t == Enum.UserInputType.Touch then
		local pos = if t == Enum.UserInputType.Touch then input.Position else nil
		local cible
		if pos then
			local r = cam:ScreenPointToRay(pos.X, pos.Y)
			cible = (function()
				local ch = lp.Character
				local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
				local meilleur, dmin = nil, math.huge
				for _, v in vitrines do
					if hrp and (hrp.Position - v.centre.Position).Magnitude < REGLAGES.DISTANCE then
						local d = rayonBoite(r.Origin, r.Direction * 200, v.centre, v.taille + Vector3.new(0.5, 0.5, 0.5))
						if d and d < dmin then meilleur, dmin = v, d end
					end
				end
				return meilleur
			end)()
		end
		cible = cible or etat.survol
		if cible then ouvrir(cible) end
	end
end)

-- si on meurt pendant que la fiche est ouverte
lp.CharacterAdded:Connect(function() if ouvert then fermer() end end)

---------------------------------------------------------------- Chaque image : survol (contour blanc), prix, fiche
local function viser()
	if not UIS.MouseEnabled then return nil end
	local pos = if UIS.MouseBehavior == Enum.MouseBehavior.LockCenter then cam.ViewportSize / 2 else UIS:GetMouseLocation()
	local r = cam:ViewportPointToRay(pos.X, pos.Y)
	local ch = lp.Character
	local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
	if not hrp then return nil end
	local meilleur, dmin = nil, math.huge
	for _, v in vitrines do
		if (hrp.Position - v.centre.Position).Magnitude < REGLAGES.DISTANCE then
			local d = rayonBoite(r.Origin, r.Direction * 200, v.centre, v.taille + Vector3.new(0.4, 0.4, 0.4))
			if d and d < dmin then meilleur, dmin = v, d end
		end
	end
	return meilleur
end

RunService.RenderStepped:Connect(function()
	for _, v in vitrines do majEtiquette(v) end
	if ouvert then
		UIS.MouseBehavior = Enum.MouseBehavior.Default
		UIS.MouseIconEnabled = true
		texteArgent.Text = argentTexte(argent())
		local v = vitrines[courant]
		if v then styleBouton(v) end
		return
	end
	local s = if lp:GetAttribute("IntroEnCours") then nil else viser()
	if s ~= etat.survol then
		if etat.survol then etat.survol.contour.Enabled = false end
		etat.survol = s
		if s then s.contour.Enabled = true end
	end
end)

---------------------------------------------------------------- Chargement des outils de l'établi
local function reponse(v, quoi, manque)
	if quoi == "ok" then
		son("rbxassetid://129348077985519", 0.7)
		message("✨ " .. string.upper(v.article.nom) .. " ACHETÉE ! ✨", OR, 2.2)
		if ouvert and vitrines[courant] == v then
			popBouton.Scale = 1.15
			TweenService:Create(popBouton, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Scale = 1 }):Play()
			for _, p in { popTouffes, popPortee } do
				p.Scale = 1.08
				TweenService:Create(p, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Scale = 1 }):Play()
			end
		end
	elseif quoi == "manque" then
		message("Il te manque " .. argentTexte(math.max(0, tonumber(manque) or 0)), ROUGE, 1.6)
		if ouvert then trembler(bouton) end
	end
end

for ordre, article in ARTICLES do
	task.spawn(function()
		local achat = ReplicatedStorage:WaitForChild(article.remote, 30)
		if not achat then return end -- cet outil n'est pas installé
		local modele = workspace:FindFirstChild(article.vitrine, true)
		for _ = 1, 30 do
			if modele then break end
			task.wait(1)
			modele = workspace:FindFirstChild(article.vitrine, true)
		end
		if not modele then return end
		local base = modele.PrimaryPart or modele:WaitForChild("Base", 10)
		if not base then return end
		task.wait(0.5) -- laisse toutes les pièces arriver
		local centre, taille = mesurer(modele, base)
		local v = { article = article, achat = achat, modele = modele, base = base, centre = centre, taille = taille, ordre = ordre }
		v.prix = function() return achat:GetAttribute("Prix") or 0 end
		v.contour = nouveau("Highlight", {
			Name = "Survol", Adornee = modele, FillColor = BLANC, FillTransparency = 0.88, OutlineColor = BLANC,
			OutlineTransparency = 0, DepthMode = Enum.HighlightDepthMode.Occluded, Enabled = false,
		}, modele)
		creerEtiquette(v)
		achat.OnClientEvent:Connect(function(quoi, manque) reponse(v, quoi, manque) end)
		-- E (clavier), bouton (manette / téléphone) : ouvre aussi la fiche
		local function brancher(p)
			if p:IsA("ProximityPrompt") then p.Triggered:Connect(function() ouvrir(v) end) end
		end
		for _, p in base:GetChildren() do brancher(p) end
		base.ChildAdded:Connect(brancher)
		table.insert(vitrines, v)
		table.sort(vitrines, function(a, b) return a.ordre < b.ordre end)
	end)
end

---------------------------------------------------------------- La débroussailleuse : tête qui tourne, moteur qui accélère
-- Au ralenti la tête tourne doucement. Quand on fauche ("EnCoupe" du serveur, ou tout de suite chez nous),
-- elle accélère : les fils deviennent un disque flou et l'herbe hachée + un peu de terre giclent devant.
local CollectionService = game:GetService("CollectionService")
local RALENTI, PLEIN_GAZ = 14, 75 -- vitesse de la tête (radians par seconde)
local tetes = setmetatable({}, { __mode = "k" }) -- [moteur] = { angle, vitesse, disque, fils, herbe, terre }

local function outilDe(m)
	local o = m:FindFirstAncestorOfClass("Tool")
	if o then return o end
	-- la copie affichée à la 1re personne (CiseauxVue) : c'est notre outil en main
	local ch = lp.Character
	return ch and ch:FindFirstChildOfClass("Tool")
end
local function enCoupe(o)
	if not o then return false end
	if o:GetAttribute("EnCoupe") then return true end
	local t = o:GetAttribute("CoupeLocale") -- mis par CiseauxVue dès notre clic (pas d'attente du serveur)
	return t ~= nil and os.clock() - t < 0.5
end
local function emetteur(parent, couleurs, taille, vitesse, taux)
	local e = Instance.new("ParticleEmitter")
	e.Texture = "rbxasset://textures/particles/sparkles_main.dds"
	e.Color = ColorSequence.new(couleurs[1], couleurs[2])
	e.Size = NumberSequence.new(taille, taille * 0.4)
	e.Squash = NumberSequence.new(1.6)
	e.Orientation = Enum.ParticleOrientation.VelocityParallel
	e.Lifetime = NumberRange.new(0.3, 0.55)
	e.Speed = NumberRange.new(vitesse * 0.7, vitesse)
	e.SpreadAngle = Vector2.new(70, 35)
	e.Acceleration = Vector3.new(0, -45, 0)
	e.Drag = 3
	e.RotSpeed = NumberRange.new(-300, 300)
	e.LightEmission = 0
	e.LightInfluence = 1
	e.Rate = 0
	e:SetAttribute("Taux", taux)
	e.Parent = parent
	return e
end
local function preparer(m)
	local pivot, handle = m.Part1, m.Part0
	if not (pivot and handle) then return nil end
	local modele = pivot.Parent
	-- copie de l'outil (vue 1re personne) : on enlève le disque et les projections copiés, on refait les nôtres
	for _, d in modele:GetChildren() do if d.Name == "DisqueFlou" then d:Destroy() end end
	for _, d in handle:GetChildren() do if d.Name == "Projections" then d:Destroy() end end
	local fils = {}
	local bobine
	for _, d in modele:GetChildren() do
		if d:IsA("BasePart") and d:GetAttribute("Tete") then
			if d.Name == "Fil" then table.insert(fils, d) elseif d.Name == "Bobine" then bobine = d end
		end
	end
	-- disque flou (les fils qui tournent très vite)
	local long = fils[1] and fils[1].Size.X or 1
	local disque = Instance.new("Part")
	disque.Name = "DisqueFlou"
	disque.Shape = Enum.PartType.Cylinder
	disque.Size = Vector3.new(0.03, long, long)
	disque.Color = Color3.fromRGB(245, 220, 120)
	disque.Material = Enum.Material.SmoothPlastic
	disque.Transparency = 1
	disque.CanCollide, disque.CanTouch, disque.CanQuery, disque.CastShadow = false, false, false, false
	disque.Massless = true
	disque.CFrame = (fils[1] and fils[1].CFrame or pivot.CFrame) * CFrame.Angles(0, 0, math.rad(90)) -- à plat, comme les fils
	local w = Instance.new("Weld")
	w.Part0, w.Part1 = pivot, disque
	w.C0 = pivot.CFrame:ToObjectSpace(disque.CFrame)
	w.Parent = disque
	disque.Parent = modele
	-- projections : repère fixe sur l'outil (pas sur la tête qui tourne), dirigé vers l'avant de la tête
	local at = Instance.new("Attachment")
	at.Name = "Projections"
	at.CFrame = m.C0 * CFrame.Angles(0, 0, math.rad(-90))
	at.Parent = handle
	local herbe = emetteur(at, { Color3.fromRGB(150, 220, 90), Color3.fromRGB(70, 150, 50) }, 0.3, 22, 150)
	local terre = emetteur(at, { Color3.fromRGB(122, 90, 58), Color3.fromRGB(86, 60, 36) }, 0.18, 14, 25)
	local e = { angle = math.random() * 6.28, vitesse = RALENTI, disque = disque, fils = fils, bobine = bobine, herbe = herbe, terre = terre }
	tetes[m] = e
	return e
end

RunService.RenderStepped:Connect(function(dt)
	for _, m in CollectionService:GetTagged("TeteDebroussailleuse") do
		-- seulement quand elle est en main (dans le monde), pas dans le sac
		if m:IsA("Motor6D") and m:IsDescendantOf(workspace) then
			local e = tetes[m] or preparer(m)
			if e then
				local coupe = enCoupe(outilDe(m))
				local cible = coupe and PLEIN_GAZ or RALENTI
				e.vitesse += (cible - e.vitesse) * math.min(dt * (coupe and 10 or 3), 1) -- ça monte vite, ça redescend doucement
				e.angle = (e.angle + e.vitesse * dt) % (2 * math.pi)
				m.Transform = CFrame.Angles(0, e.angle, 0)
				local k = math.clamp((e.vitesse - RALENTI) / (PLEIN_GAZ - RALENTI), 0, 1) -- 0 = ralenti, 1 = plein gaz
				local cache = e.bobine and e.bobine.LocalTransparencyModifier or 0 -- caché à la 1re personne (vraie débroussailleuse)
				e.disque.Transparency = 1 - 0.35 * k
				e.disque.LocalTransparencyModifier = cache
				for _, f in e.fils do f.LocalTransparencyModifier = math.max(cache, k > 0.5 and 1 or 0) end
				local actif = coupe and cache < 1
				e.herbe.Rate = actif and e.herbe:GetAttribute("Taux") or 0
				e.terre.Rate = actif and e.terre:GetAttribute("Taux") or 0
			end
		end
	end
end)

]]></ProtectedString></Properties></Item></roblox>